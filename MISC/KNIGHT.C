/* KNIGHT'S MOVE */#include <stdio.h>#include <stdlib.h>#include <time.h>#define white '\xdb'#define black '\xb0'#define sign '\x03'main(){	int x,y,i,xx,yy,tempx,tempy,indecator[8][8]={0};	clock_t start,end;
	clrscr();	start = clock();
	for (y=1;y<=24;y++)		for (x=1;x<=64;x++){			gotoxy(x,y);			if ((y%6<4)&&(y%6>0)) {				if ((x%16<9)&&(x%16>0)) printf("%c",white);				else printf("%c",black);
			}			else {				if ((x%16<9)&&(x%16>0)) printf("%c",black);				else printf("%c",white); }			}		gotoxy(24,25);printf("KNIGHT'S TOUR");		gotoxy(52,23);printf("%c%c",sign,sign);		tempx=6;tempy=7;		indecator[tempx][tempy]=1;		delay(1000);		i=1;		randomize();		while (i<64) {			x=random(4);			if (x<2) y=random(2)+2;			else y=random(2);			xx=tempx;yy=tempy;			switch (x) {				case 0:tempx+=1;break;				case 1:tempx-=1;break;				case 2:tempx+=2;break;				case 3:tempx-=2;break;			}			switch (y) {				case 0:tempy+=1;break;				case 1:tempy-=1;break;				case 2:tempy+=2;break;				case 3:tempy-=2;break;			}			if ((tempx>=0 && tempx<=7) && (tempy>=0 && tempy<=7) && (indecator[tempx][tempy]==0)) {			indecator[tempx][tempy]=1;			gotoxy((tempx+1)*8-4,(tempy+1)*3-1);			printf("%c%c",sign,sign);i++;			delay(1000);			start=clock();		} else {   tempx=xx;   tempy=yy;  }  end=clock();  if ((end-start)/CLK_TCK>1.0) {   gotoxy(40,25);printf("Total %d movement done.",i-1);   i=64;  } } getch();}